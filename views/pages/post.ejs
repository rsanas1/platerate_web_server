<!DOCTYPE html>
<html lang="en">
<head>
   <title>POSTS</title>
   
</head>
<body class="container">

   
    <main>
        <ol class="Center">
            <div >
                <% notes.forEach(function(note) { %>
                    <li class = "title"  ><%= note.title %> </li>
                    <li class = "desc" ><%= note.body %></li>
                <% }); %>
            </div>
        </ol>
    </main>

    <% include ../partials/head %>

</body>


</html>